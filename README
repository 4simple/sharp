# sharp

A sharp static server based on KOA.

## install

```shell
npm install -g sharpserver
```

## usage

1. cd directory

```shell
cd ~/myfiles
```

2. start server
```shell
sharp -p 80 -r static
```

3. visit

visit 'http://localhost/static'
